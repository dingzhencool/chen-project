fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-jose[cryptography]==3.3.0
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
SQLAlchemy==2.0.35
PyMySQL==1.1.1
Alembic==1.13.2
redis==5.0.8
httpx==0.27.2
python-dotenv==1.0.1
aiofiles==24.1.0
# === RAG 必需依赖 ===
Pillow>=10.0.0                 # 图片解析（vision.py），图片入库/问答必需

# === RAG 可选依赖（推荐安装，未安装时自动降级为内存/Mock 实现）===
# chromadb>=0.5.0               # 持久化向量存储；未安装 → 使用内存向量库（重启后数据丢失）
# pymupdf>=1.24.0               # PDF 真实解析；未安装 → PDF 降级为占位文本
# python-docx>=1.1.0            # DOCX 真实解析；未安装 → DOCX 降级为占位文本
# sentence-transformers>=2.7.0  # 本地 Embedding；未安装 + 未配 LLM_API_KEY → Mock Embedding
